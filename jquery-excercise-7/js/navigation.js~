$('document').ready(function(){
  $list = $('ul#nav')
  $list.children().each(function(index){  
  $list.children().eq(index).hover(
function(){ console.log($(this).find('ul'));$(this).find('ul').show() }, function(){ $(this).children().find('ul').hide() })  
 })



});
